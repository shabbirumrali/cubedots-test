import data from '../data/test_units_data.json'

// const data = JSON.parse(data)

const UnitData = (state = data, action) => {
    switch(action.type) {        
        default:
            return state
    }
}

export default UnitData