import testData from '../data/test_fliter_options.json'

const optionFilter = (state = testData, action) => {
    switch(action.type) {        
        default:
            return state
    }
}
export default optionFilter